{
  "name": "youra-sleep",
  "private": true,
  "scripts": {
    "build": "esbuild app/javascript/application.js --bundle --sourcemap --outfile=app/assets/builds/application.js",
    "watch": "esbuild app/javascript/application.js --bundle --sourcemap --outfile=app/assets/builds/application.js --watch"
  },
  "dependencies": {
    "@hotwired/stimulus": "^3.2.2",
    "@hotwired/turbo-rails": "^8.0.0"
  },
  "devDependencies": {
    "esbuild": "^0.24.0"
  }
}
