<div class="max-w-5xl mx-auto p-6">
    <div class="flex items-center justify-between">
        <h1 class="text-xl font-semibold"><%= @payload.data_type %></h1>
        <%= link_to "Back", oura_raw_payloads_path, class: "underline" %>
    </div>

    <pre class="mt-4 p-4 rounded bg-gray-100 overflow-auto text-sm"><%= JSON.pretty_generate(@payload.payload) %></pre>
</div>